# coding=UTF-8
# ex:ts=4:sw=4:et=on

from PySide6.QtGui import QColor


def parse_color(value):
    """Convert a color string (e.g. '#FF0000' or 'red') to a QColor."""
    if isinstance(value, QColor):
        return value
    return QColor(str(value))


def color_to_string(color):
    """Convert a QColor to a '#rrggbb' hex string."""
    if isinstance(color, QColor):
        return color.name()
    return str(color)
